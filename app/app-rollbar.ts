// import { Client, Configuration } from 'rollbar-react-native'
// import { AppConfig } from './app-config'

// const config = new Configuration(AppConfig.rollbarToken, {})

// export const rollbar = new Client(config)
